//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMPTUOBJECTS_PTUMOVEFLAG_ENUM_H_
#define COMMPTUOBJECTS_PTUMOVEFLAG_ENUM_H_

#include <CommPTUObjects/enumPTUMoveFlagData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommPTUObjects {
	
	class PTUMoveFlag {
	private:
		CommPTUObjectsIDL::PTUMoveFlag value;
		
	public:
		enum ENUM_PTUMoveFlag {
			ENUM_VALUE_UNDEFINED = 0,
			PAN_RELATIVE = 1,
			PAN_ABSOLUTE = 2,
			TILT_RELATIVE = 3,
			TILT_ABSOLUTE = 4,
			PAN_TILT_RELATIVE = 5,
			PAN_TILT_ABSOLUTE = 6,
			POSITION_ROBOT = 7,
			POSITION_WORLD = 8
		};
		
		// default constructor
		PTUMoveFlag() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		PTUMoveFlag(ENUM_PTUMoveFlag e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		PTUMoveFlag(CommPTUObjectsIDL::PTUMoveFlag e) {
			value = e;
		}
		
		//const CommPTUObjectsIDL::PTUMoveFlag& get() const { return value; }
		
		// ENUM operator
		operator ENUM_PTUMoveFlag() const {
			return static_cast<ENUM_PTUMoveFlag>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_PTUMoveFlag t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case PAN_RELATIVE:
					result = "PTUMoveFlag::PAN_RELATIVE";
					break;
				case PAN_ABSOLUTE:
					result = "PTUMoveFlag::PAN_ABSOLUTE";
					break;
				case TILT_RELATIVE:
					result = "PTUMoveFlag::TILT_RELATIVE";
					break;
				case TILT_ABSOLUTE:
					result = "PTUMoveFlag::TILT_ABSOLUTE";
					break;
				case PAN_TILT_RELATIVE:
					result = "PTUMoveFlag::PAN_TILT_RELATIVE";
					break;
				case PAN_TILT_ABSOLUTE:
					result = "PTUMoveFlag::PAN_TILT_ABSOLUTE";
					break;
				case POSITION_ROBOT:
					result = "PTUMoveFlag::POSITION_ROBOT";
					break;
				case POSITION_WORLD:
					result = "PTUMoveFlag::POSITION_WORLD";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const PTUMoveFlag &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
