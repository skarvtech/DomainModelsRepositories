//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef DOMAINSPEECH_SPEECHOUTPUTTAGTYPE_ENUM_H_
#define DOMAINSPEECH_SPEECHOUTPUTTAGTYPE_ENUM_H_

#include <DomainSpeech/enumSpeechOutputTagTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace DomainSpeech {
	
	class SpeechOutputTagType {
	private:
		DomainSpeechIDL::SpeechOutputTagType value;
		
	public:
		enum ENUM_SpeechOutputTagType {
			ENUM_VALUE_UNDEFINED = 0,
			SPEECH_SET_VOLUME = 1
		};
		
		// default constructor
		SpeechOutputTagType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		SpeechOutputTagType(ENUM_SpeechOutputTagType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		SpeechOutputTagType(DomainSpeechIDL::SpeechOutputTagType e) {
			value = e;
		}
		
		//const DomainSpeechIDL::SpeechOutputTagType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_SpeechOutputTagType() const {
			return static_cast<ENUM_SpeechOutputTagType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_SpeechOutputTagType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case SPEECH_SET_VOLUME:
					result = "SpeechOutputTagType::SPEECH_SET_VOLUME";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const SpeechOutputTagType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
