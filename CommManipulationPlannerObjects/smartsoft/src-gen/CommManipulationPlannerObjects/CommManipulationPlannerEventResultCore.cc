//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommManipulationPlannerObjects/CommManipulationPlannerEventResultCore.hh"

// serialization/deserialization operators
//#include "CommManipulationPlannerObjects/CommManipulationPlannerEventResultACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommManipulationPlannerObjects 
{
	const char* CommManipulationPlannerEventResultCore::getCompiledHash()
	{
		return CommManipulationPlannerObjectsIDL::REPO_HASH;
	}
	
	void CommManipulationPlannerEventResultCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommBasicObjects::CommPose3d(idl_CommManipulationPlannerEventResult.pose)
		CommBasicObjects::CommPose3d::getAllHashValues(hashes);
	}
	
	void CommManipulationPlannerEventResultCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommManipulationPlannerEventResultCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommBasicObjects::CommPose3d(idl_CommManipulationPlannerEventResult.pose)
		CommBasicObjects::CommPose3d::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommManipulationPlannerEventResultCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.event);
		seed += CommBasicObjects::CommPose3d::generateDataHash(data.pose);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommManipulationPlannerEventResultCore::CommManipulationPlannerEventResultCore()
	:	idl_CommManipulationPlannerEventResult()
	{  
		setEvent(CommManipulationPlannerObjects::ManipulationPlannerEvent());
		setPose(CommBasicObjects::CommPose3d());
	}
	
	CommManipulationPlannerEventResultCore::CommManipulationPlannerEventResultCore(const DATATYPE &data)
	:	idl_CommManipulationPlannerEventResult(data)
	{  }

	CommManipulationPlannerEventResultCore::~CommManipulationPlannerEventResultCore()
	{  }
	
	void CommManipulationPlannerEventResultCore::to_ostream(std::ostream &os) const
	{
	  os << "CommManipulationPlannerEventResult(";
	  getEvent().to_ostream(os);
	  getPose().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommManipulationPlannerEventResultCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<event>";
		getEvent().to_xml(os, indent);
		os << indent << "</event>";
		os << indent << "<pose>";
		getPose().to_xml(os, indent);
		os << indent << "</pose>";
	}
	
	// restore from xml stream
	void CommManipulationPlannerEventResultCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_event("<event>");
		static const Smart::KnuthMorrisPratt kmp_pose("<pose>");
		
		if(kmp_event.search(is)) {
			CommManipulationPlannerObjects::ManipulationPlannerEvent eventItem;
			eventItem.from_xml(is);
			setEvent(eventItem);
		}
		if(kmp_pose.search(is)) {
			CommBasicObjects::CommPose3d poseItem;
			poseItem.from_xml(is);
			setPose(poseItem);
		}
	}
	
	/*
	void CommManipulationPlannerEventResultCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommManipulationPlannerObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommManipulationPlannerObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommManipulationPlannerEventResultACE.hh)
		cdr << idl_CommManipulationPlannerEventResult;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommManipulationPlannerEventResult);
		cdr << data_hash;
		// std::cout << "CommManipulationPlannerEventResultCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommManipulationPlannerEventResultCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommManipulationPlannerObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommManipulationPlannerEventResultACE.hh)
		cdr >> idl_CommManipulationPlannerEventResult;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommManipulationPlannerEventResult);
		assert(data_hash == own_hash);
		// std::cout << "CommManipulationPlannerEventResultCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommManipulationPlannerObjects */
