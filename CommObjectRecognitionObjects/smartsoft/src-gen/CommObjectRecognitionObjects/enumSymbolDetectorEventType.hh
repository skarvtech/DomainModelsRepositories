//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMOBJECTRECOGNITIONOBJECTS_SYMBOLDETECTOREVENTTYPE_ENUM_H_
#define COMMOBJECTRECOGNITIONOBJECTS_SYMBOLDETECTOREVENTTYPE_ENUM_H_

#include <CommObjectRecognitionObjects/enumSymbolDetectorEventTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommObjectRecognitionObjects {
	
	class SymbolDetectorEventType {
	private:
		CommObjectRecognitionObjectsIDL::SymbolDetectorEventType value;
		
	public:
		enum ENUM_SymbolDetectorEventType {
			ENUM_VALUE_UNDEFINED = 0,
			SYMBOL_UNKNOWN = 1,
			SYMBOL_OBJECT_UNKNOWN = 2,
			SYMBOL_OBJECT_FOUND = 3,
			SYMBOL_OBJECT_NOT_FOUND = 4,
			SYMBOL_UNABLE_TO_GET_POS = 5
		};
		
		// default constructor
		SymbolDetectorEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		SymbolDetectorEventType(ENUM_SymbolDetectorEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		SymbolDetectorEventType(CommObjectRecognitionObjectsIDL::SymbolDetectorEventType e) {
			value = e;
		}
		
		//const CommObjectRecognitionObjectsIDL::SymbolDetectorEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_SymbolDetectorEventType() const {
			return static_cast<ENUM_SymbolDetectorEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_SymbolDetectorEventType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case SYMBOL_UNKNOWN:
					result = "SymbolDetectorEventType::SYMBOL_UNKNOWN";
					break;
				case SYMBOL_OBJECT_UNKNOWN:
					result = "SymbolDetectorEventType::SYMBOL_OBJECT_UNKNOWN";
					break;
				case SYMBOL_OBJECT_FOUND:
					result = "SymbolDetectorEventType::SYMBOL_OBJECT_FOUND";
					break;
				case SYMBOL_OBJECT_NOT_FOUND:
					result = "SymbolDetectorEventType::SYMBOL_OBJECT_NOT_FOUND";
					break;
				case SYMBOL_UNABLE_TO_GET_POS:
					result = "SymbolDetectorEventType::SYMBOL_UNABLE_TO_GET_POS";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const SymbolDetectorEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
