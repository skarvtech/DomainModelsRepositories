//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommManipulatorObjects/CommMobileManipulatorStateCore.hh"

// serialization/deserialization operators
//#include "CommManipulatorObjects/CommMobileManipulatorStateACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommManipulatorObjects 
{
	const char* CommMobileManipulatorStateCore::getCompiledHash()
	{
		return CommManipulatorObjectsIDL::REPO_HASH;
	}
	
	void CommMobileManipulatorStateCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommManipulatorObjects::CommManipulatorState(idl_CommMobileManipulatorState.manipulator_state)
		CommManipulatorObjects::CommManipulatorState::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommBaseState(idl_CommMobileManipulatorState.base_state)
		CommBasicObjects::CommBaseState::getAllHashValues(hashes);
	}
	
	void CommMobileManipulatorStateCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommMobileManipulatorStateCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommManipulatorObjects::CommManipulatorState(idl_CommMobileManipulatorState.manipulator_state)
		CommManipulatorObjects::CommManipulatorState::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommBaseState(idl_CommMobileManipulatorState.base_state)
		CommBasicObjects::CommBaseState::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommMobileManipulatorStateCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += CommManipulatorObjects::CommManipulatorState::generateDataHash(data.manipulator_state);
		seed += CommBasicObjects::CommBaseState::generateDataHash(data.base_state);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommMobileManipulatorStateCore::CommMobileManipulatorStateCore()
	:	idl_CommMobileManipulatorState()
	{  
		setManipulator_state(CommManipulatorObjects::CommManipulatorState());
		setBase_state(CommBasicObjects::CommBaseState());
	}
	
	CommMobileManipulatorStateCore::CommMobileManipulatorStateCore(const DATATYPE &data)
	:	idl_CommMobileManipulatorState(data)
	{  }

	CommMobileManipulatorStateCore::~CommMobileManipulatorStateCore()
	{  }
	
	void CommMobileManipulatorStateCore::to_ostream(std::ostream &os) const
	{
	  os << "CommMobileManipulatorState(";
	  getManipulator_state().to_ostream(os);
	  getBase_state().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommMobileManipulatorStateCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<manipulator_state>";
		getManipulator_state().to_xml(os, indent);
		os << indent << "</manipulator_state>";
		os << indent << "<base_state>";
		getBase_state().to_xml(os, indent);
		os << indent << "</base_state>";
	}
	
	// restore from xml stream
	void CommMobileManipulatorStateCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_manipulator_state("<manipulator_state>");
		static const Smart::KnuthMorrisPratt kmp_base_state("<base_state>");
		
		if(kmp_manipulator_state.search(is)) {
			CommManipulatorObjects::CommManipulatorState manipulator_stateItem;
			manipulator_stateItem.from_xml(is);
			setManipulator_state(manipulator_stateItem);
		}
		if(kmp_base_state.search(is)) {
			CommBasicObjects::CommBaseState base_stateItem;
			base_stateItem.from_xml(is);
			setBase_state(base_stateItem);
		}
	}
	
	/*
	void CommMobileManipulatorStateCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommManipulatorObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommManipulatorObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommMobileManipulatorStateACE.hh)
		cdr << idl_CommMobileManipulatorState;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommMobileManipulatorState);
		cdr << data_hash;
		// std::cout << "CommMobileManipulatorStateCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommMobileManipulatorStateCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommManipulatorObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommMobileManipulatorStateACE.hh)
		cdr >> idl_CommMobileManipulatorState;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommMobileManipulatorState);
		assert(data_hash == own_hash);
		// std::cout << "CommMobileManipulatorStateCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommManipulatorObjects */
