//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommTrackingObjects/PersonDetectionEventStateCore.hh"

// serialization/deserialization operators
//#include "CommTrackingObjects/PersonDetectionEventStateACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommTrackingObjects 
{
	const char* PersonDetectionEventStateCore::getCompiledHash()
	{
		return CommTrackingObjectsIDL::REPO_HASH;
	}
	
	void PersonDetectionEventStateCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
	}
	
	void PersonDetectionEventStateCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "PersonDetectionEventStateCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
	}
	
	#ifdef ENABLE_HASH
	size_t PersonDetectionEventStateCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.environment_id);
		std::vector<ACE_CDR::ULong>::const_iterator data_person_idIt;
		for(data_person_idIt=data.person_id.begin(); data_person_idIt!=data.person_id.end(); data_person_idIt++) {
			boost::hash_combine(seed, *data_person_idIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	PersonDetectionEventStateCore::PersonDetectionEventStateCore()
	:	idl_PersonDetectionEventState()
	{  
		setEnvironment_id(0);
		setPerson_id(std::vector<unsigned int>());
	}
	
	PersonDetectionEventStateCore::PersonDetectionEventStateCore(const DATATYPE &data)
	:	idl_PersonDetectionEventState(data)
	{  }

	PersonDetectionEventStateCore::~PersonDetectionEventStateCore()
	{  }
	
	void PersonDetectionEventStateCore::to_ostream(std::ostream &os) const
	{
	  os << "PersonDetectionEventState(";
	  os << getEnvironment_id() << " ";
	  std::vector<unsigned int>::const_iterator person_idIt;
	  std::vector<unsigned int> person_idList = getPerson_idCopy();
	  for(person_idIt=person_idList.begin(); person_idIt!=person_idList.end(); person_idIt++) {
	  	os << *person_idIt << " ";
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void PersonDetectionEventStateCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<environment_id>" << getEnvironment_id() << "</environment_id>";
		std::vector<unsigned int>::const_iterator person_idIt;
		std::vector<unsigned int> person_idList = getPerson_idCopy();
		counter = 0;
		os << indent << "<person_idList n=\"" << person_idList.size() << "\">";
		for(person_idIt=person_idList.begin(); person_idIt!=person_idList.end(); person_idIt++) {
			os << indent << "<person_id i=\"" << counter++ << "\">" << *person_idIt << "</person_id>";
		}
		os << indent << "</person_idList>";
	}
	
	// restore from xml stream
	void PersonDetectionEventStateCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_environment_id("<environment_id>");
		static const Smart::KnuthMorrisPratt kmp_person_idList("<person_idList n=\"");
		static const Smart::KnuthMorrisPratt kmp_person_id("\">");
		
		if(kmp_environment_id.search(is)) {
			unsigned int environment_idItem;
			is >> environment_idItem;
			setEnvironment_id(environment_idItem);
		}
		if(kmp_person_idList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			unsigned int person_idItem;
			std::vector<unsigned int> person_idList;
			kmp_person_id.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_person_id.search(is)) {
					is >> person_idItem;
					person_idList.push_back(person_idItem);
				}
			}
			setPerson_id(person_idList);
		}
	}
	
	/*
	void PersonDetectionEventStateCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommTrackingObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommTrackingObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see PersonDetectionEventStateACE.hh)
		cdr << idl_PersonDetectionEventState;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_PersonDetectionEventState);
		cdr << data_hash;
		// std::cout << "PersonDetectionEventStateCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void PersonDetectionEventStateCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommTrackingObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see PersonDetectionEventStateACE.hh)
		cdr >> idl_PersonDetectionEventState;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_PersonDetectionEventState);
		assert(data_hash == own_hash);
		// std::cout << "PersonDetectionEventStateCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommTrackingObjects */
