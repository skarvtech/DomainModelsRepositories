//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommNavigationObjects/CommGridMapRequestCore.hh"

// serialization/deserialization operators
//#include "CommNavigationObjects/CommGridMapRequestACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommNavigationObjects 
{
	const char* CommGridMapRequestCore::getCompiledHash()
	{
		return CommNavigationObjectsIDL::REPO_HASH;
	}
	
	void CommGridMapRequestCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
	}
	
	void CommGridMapRequestCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommGridMapRequestCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
	}
	
	#ifdef ENABLE_HASH
	size_t CommGridMapRequestCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.id);
		boost::hash_combine(seed, data.xOffsetMM);
		boost::hash_combine(seed, data.yOffsetMM);
		boost::hash_combine(seed, data.xSizeMM);
		boost::hash_combine(seed, data.ySizeMM);
		boost::hash_combine(seed, data.curLtmState);
		boost::hash_combine(seed, data.curLtmThreshold);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommGridMapRequestCore::CommGridMapRequestCore()
	:	idl_CommGridMapRequest()
	{  
		setId(0);
		setXOffsetMM(0);
		setYOffsetMM(0);
		setXSizeMM(0);
		setYSizeMM(0);
		setCurLtmState(false);
		setCurLtmThreshold(0);
	}
	
	CommGridMapRequestCore::CommGridMapRequestCore(const DATATYPE &data)
	:	idl_CommGridMapRequest(data)
	{  }

	CommGridMapRequestCore::~CommGridMapRequestCore()
	{  }
	
	void CommGridMapRequestCore::to_ostream(std::ostream &os) const
	{
	  os << "CommGridMapRequest(";
	  os << getId() << " ";
	  os << getXOffsetMM() << " ";
	  os << getYOffsetMM() << " ";
	  os << getXSizeMM() << " ";
	  os << getYSizeMM() << " ";
	  os << getCurLtmState() << " ";
	  os << getCurLtmThreshold() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void CommGridMapRequestCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<id>" << getId() << "</id>";
		os << indent << "<xOffsetMM>" << getXOffsetMM() << "</xOffsetMM>";
		os << indent << "<yOffsetMM>" << getYOffsetMM() << "</yOffsetMM>";
		os << indent << "<xSizeMM>" << getXSizeMM() << "</xSizeMM>";
		os << indent << "<ySizeMM>" << getYSizeMM() << "</ySizeMM>";
		os << indent << "<curLtmState>" << getCurLtmState() << "</curLtmState>";
		os << indent << "<curLtmThreshold>" << getCurLtmThreshold() << "</curLtmThreshold>";
	}
	
	// restore from xml stream
	void CommGridMapRequestCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_id("<id>");
		static const Smart::KnuthMorrisPratt kmp_xOffsetMM("<xOffsetMM>");
		static const Smart::KnuthMorrisPratt kmp_yOffsetMM("<yOffsetMM>");
		static const Smart::KnuthMorrisPratt kmp_xSizeMM("<xSizeMM>");
		static const Smart::KnuthMorrisPratt kmp_ySizeMM("<ySizeMM>");
		static const Smart::KnuthMorrisPratt kmp_curLtmState("<curLtmState>");
		static const Smart::KnuthMorrisPratt kmp_curLtmThreshold("<curLtmThreshold>");
		
		if(kmp_id.search(is)) {
			unsigned int idItem;
			is >> idItem;
			setId(idItem);
		}
		if(kmp_xOffsetMM.search(is)) {
			int xOffsetMMItem;
			is >> xOffsetMMItem;
			setXOffsetMM(xOffsetMMItem);
		}
		if(kmp_yOffsetMM.search(is)) {
			int yOffsetMMItem;
			is >> yOffsetMMItem;
			setYOffsetMM(yOffsetMMItem);
		}
		if(kmp_xSizeMM.search(is)) {
			unsigned int xSizeMMItem;
			is >> xSizeMMItem;
			setXSizeMM(xSizeMMItem);
		}
		if(kmp_ySizeMM.search(is)) {
			unsigned int ySizeMMItem;
			is >> ySizeMMItem;
			setYSizeMM(ySizeMMItem);
		}
		if(kmp_curLtmState.search(is)) {
			bool curLtmStateItem;
			is >> curLtmStateItem;
			setCurLtmState(curLtmStateItem);
		}
		if(kmp_curLtmThreshold.search(is)) {
			unsigned int curLtmThresholdItem;
			is >> curLtmThresholdItem;
			setCurLtmThreshold(curLtmThresholdItem);
		}
	}
	
	/*
	void CommGridMapRequestCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommNavigationObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommNavigationObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommGridMapRequestACE.hh)
		cdr << idl_CommGridMapRequest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommGridMapRequest);
		cdr << data_hash;
		// std::cout << "CommGridMapRequestCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommGridMapRequestCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommNavigationObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommGridMapRequestACE.hh)
		cdr >> idl_CommGridMapRequest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommGridMapRequest);
		assert(data_hash == own_hash);
		// std::cout << "CommGridMapRequestCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommNavigationObjects */
