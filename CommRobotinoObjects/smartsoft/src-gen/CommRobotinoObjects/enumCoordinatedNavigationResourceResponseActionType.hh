//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMROBOTINOOBJECTS_COORDINATEDNAVIGATIONRESOURCERESPONSEACTIONTYPE_ENUM_H_
#define COMMROBOTINOOBJECTS_COORDINATEDNAVIGATIONRESOURCERESPONSEACTIONTYPE_ENUM_H_

#include <CommRobotinoObjects/enumCoordinatedNavigationResourceResponseActionTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommRobotinoObjects {
	
	class CoordinatedNavigationResourceResponseActionType {
	private:
		CommRobotinoObjectsIDL::CoordinatedNavigationResourceResponseActionType value;
		
	public:
		enum ENUM_CoordinatedNavigationResourceResponseActionType {
			ENUM_VALUE_UNDEFINED = 0,
			UNKOWN = 1,
			SUCCESS = 2,
			FAILED = 3
		};
		
		// default constructor
		CoordinatedNavigationResourceResponseActionType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		CoordinatedNavigationResourceResponseActionType(ENUM_CoordinatedNavigationResourceResponseActionType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		CoordinatedNavigationResourceResponseActionType(CommRobotinoObjectsIDL::CoordinatedNavigationResourceResponseActionType e) {
			value = e;
		}
		
		//const CommRobotinoObjectsIDL::CoordinatedNavigationResourceResponseActionType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_CoordinatedNavigationResourceResponseActionType() const {
			return static_cast<ENUM_CoordinatedNavigationResourceResponseActionType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_CoordinatedNavigationResourceResponseActionType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case UNKOWN:
					result = "CoordinatedNavigationResourceResponseActionType::UNKOWN";
					break;
				case SUCCESS:
					result = "CoordinatedNavigationResourceResponseActionType::SUCCESS";
					break;
				case FAILED:
					result = "CoordinatedNavigationResourceResponseActionType::FAILED";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const CoordinatedNavigationResourceResponseActionType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
