//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommRobotinoObjects/CommDigitalInputEventStateCore.hh"

// serialization/deserialization operators
//#include "CommRobotinoObjects/CommDigitalInputEventStateACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommRobotinoObjects 
{
	const char* CommDigitalInputEventStateCore::getCompiledHash()
	{
		return CommRobotinoObjectsIDL::REPO_HASH;
	}
	
	void CommDigitalInputEventStateCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
	}
	
	void CommDigitalInputEventStateCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommDigitalInputEventStateCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
	}
	
	#ifdef ENABLE_HASH
	size_t CommDigitalInputEventStateCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		std::vector<ACE_CDR::Boolean>::const_iterator data_digitalInputValuesIt;
		for(data_digitalInputValuesIt=data.digitalInputValues.begin(); data_digitalInputValuesIt!=data.digitalInputValues.end(); data_digitalInputValuesIt++) {
			boost::hash_combine(seed, *data_digitalInputValuesIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	CommDigitalInputEventStateCore::CommDigitalInputEventStateCore()
	:	idl_CommDigitalInputEventState()
	{  
		setDigitalInputValues(std::vector<bool>());
	}
	
	CommDigitalInputEventStateCore::CommDigitalInputEventStateCore(const DATATYPE &data)
	:	idl_CommDigitalInputEventState(data)
	{  }

	CommDigitalInputEventStateCore::~CommDigitalInputEventStateCore()
	{  }
	
	void CommDigitalInputEventStateCore::to_ostream(std::ostream &os) const
	{
	  os << "CommDigitalInputEventState(";
	  std::vector<bool>::const_iterator digitalInputValuesIt;
	  std::vector<bool> digitalInputValuesList = getDigitalInputValuesCopy();
	  for(digitalInputValuesIt=digitalInputValuesList.begin(); digitalInputValuesIt!=digitalInputValuesList.end(); digitalInputValuesIt++) {
	  	os << *digitalInputValuesIt << " ";
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void CommDigitalInputEventStateCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		std::vector<bool>::const_iterator digitalInputValuesIt;
		std::vector<bool> digitalInputValuesList = getDigitalInputValuesCopy();
		counter = 0;
		os << indent << "<digitalInputValuesList n=\"" << digitalInputValuesList.size() << "\">";
		for(digitalInputValuesIt=digitalInputValuesList.begin(); digitalInputValuesIt!=digitalInputValuesList.end(); digitalInputValuesIt++) {
			os << indent << "<digitalInputValues i=\"" << counter++ << "\">" << *digitalInputValuesIt << "</digitalInputValues>";
		}
		os << indent << "</digitalInputValuesList>";
	}
	
	// restore from xml stream
	void CommDigitalInputEventStateCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_digitalInputValuesList("<digitalInputValuesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_digitalInputValues("\">");
		
		if(kmp_digitalInputValuesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			bool digitalInputValuesItem;
			std::vector<bool> digitalInputValuesList;
			kmp_digitalInputValues.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_digitalInputValues.search(is)) {
					is >> digitalInputValuesItem;
					digitalInputValuesList.push_back(digitalInputValuesItem);
				}
			}
			setDigitalInputValues(digitalInputValuesList);
		}
	}
	
	/*
	void CommDigitalInputEventStateCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommRobotinoObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommRobotinoObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommDigitalInputEventStateACE.hh)
		cdr << idl_CommDigitalInputEventState;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommDigitalInputEventState);
		cdr << data_hash;
		// std::cout << "CommDigitalInputEventStateCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommDigitalInputEventStateCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommRobotinoObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommDigitalInputEventStateACE.hh)
		cdr >> idl_CommDigitalInputEventState;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommDigitalInputEventState);
		assert(data_hash == own_hash);
		// std::cout << "CommDigitalInputEventStateCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommRobotinoObjects */
