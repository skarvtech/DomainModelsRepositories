//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMROBOTINOOBJECTS_ROBOTINODOCKINGEVENTTYPE_ENUM_H_
#define COMMROBOTINOOBJECTS_ROBOTINODOCKINGEVENTTYPE_ENUM_H_

#include <CommRobotinoObjects/enumRobotinoDockingEventTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommRobotinoObjects {
	
	class RobotinoDockingEventType {
	private:
		CommRobotinoObjectsIDL::RobotinoDockingEventType value;
		
	public:
		enum ENUM_RobotinoDockingEventType {
			ENUM_VALUE_UNDEFINED = 0,
			DOCKING_UNKNOWN = 1,
			IR_DOCKING_DONE = 2,
			LASER_DOCKING_DONE = 3,
			IR_DOCKING_NOT_DONE = 4,
			LASER_DOCKING_NOT_DONE = 5,
			LASER_DOCKING_ERROR = 6,
			IR_DOCKING_ERROR = 7,
			UN_DOCKING_NOT_DONE = 8,
			UN_DOCKING_DONE = 9,
			UN_DOCKING_ERROR = 10
		};
		
		// default constructor
		RobotinoDockingEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		RobotinoDockingEventType(ENUM_RobotinoDockingEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		RobotinoDockingEventType(CommRobotinoObjectsIDL::RobotinoDockingEventType e) {
			value = e;
		}
		
		//const CommRobotinoObjectsIDL::RobotinoDockingEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_RobotinoDockingEventType() const {
			return static_cast<ENUM_RobotinoDockingEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_RobotinoDockingEventType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case DOCKING_UNKNOWN:
					result = "RobotinoDockingEventType::DOCKING_UNKNOWN";
					break;
				case IR_DOCKING_DONE:
					result = "RobotinoDockingEventType::IR_DOCKING_DONE";
					break;
				case LASER_DOCKING_DONE:
					result = "RobotinoDockingEventType::LASER_DOCKING_DONE";
					break;
				case IR_DOCKING_NOT_DONE:
					result = "RobotinoDockingEventType::IR_DOCKING_NOT_DONE";
					break;
				case LASER_DOCKING_NOT_DONE:
					result = "RobotinoDockingEventType::LASER_DOCKING_NOT_DONE";
					break;
				case LASER_DOCKING_ERROR:
					result = "RobotinoDockingEventType::LASER_DOCKING_ERROR";
					break;
				case IR_DOCKING_ERROR:
					result = "RobotinoDockingEventType::IR_DOCKING_ERROR";
					break;
				case UN_DOCKING_NOT_DONE:
					result = "RobotinoDockingEventType::UN_DOCKING_NOT_DONE";
					break;
				case UN_DOCKING_DONE:
					result = "RobotinoDockingEventType::UN_DOCKING_DONE";
					break;
				case UN_DOCKING_ERROR:
					result = "RobotinoDockingEventType::UN_DOCKING_ERROR";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const RobotinoDockingEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
