//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMROBOTINOOBJECTS_NODEREQUESTANSERTYPE_ENUM_H_
#define COMMROBOTINOOBJECTS_NODEREQUESTANSERTYPE_ENUM_H_

#include <CommRobotinoObjects/enumNodeRequestAnserTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommRobotinoObjects {
	
	class NodeRequestAnserType {
	private:
		CommRobotinoObjectsIDL::NodeRequestAnserType value;
		
	public:
		enum ENUM_NodeRequestAnserType {
			ENUM_VALUE_UNDEFINED = 0,
			STATE_UNKNOWN = 1,
			SUCCESS = 2,
			FAIL = 3,
			ERROR_NONODE = 4
		};
		
		// default constructor
		NodeRequestAnserType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		NodeRequestAnserType(ENUM_NodeRequestAnserType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		NodeRequestAnserType(CommRobotinoObjectsIDL::NodeRequestAnserType e) {
			value = e;
		}
		
		//const CommRobotinoObjectsIDL::NodeRequestAnserType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_NodeRequestAnserType() const {
			return static_cast<ENUM_NodeRequestAnserType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_NodeRequestAnserType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case STATE_UNKNOWN:
					result = "NodeRequestAnserType::STATE_UNKNOWN";
					break;
				case SUCCESS:
					result = "NodeRequestAnserType::SUCCESS";
					break;
				case FAIL:
					result = "NodeRequestAnserType::FAIL";
					break;
				case ERROR_NONODE:
					result = "NodeRequestAnserType::ERROR_NONODE";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const NodeRequestAnserType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
