//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMROBOTINOOBJECTS_ROBOTINODOCKINGEVENTSTATE_CORE_H_
#define COMMROBOTINOOBJECTS_ROBOTINODOCKINGEVENTSTATE_CORE_H_

#include "CommRobotinoObjects/RobotinoDockingEventStateData.hh"
#include "CommRobotinoObjects/enumRobotinoDockingEventType.hh"

#include <iostream>
#include <string>
#include <list>

namespace CommRobotinoObjects {
	
class RobotinoDockingEventStateCore {
protected:
	// data structure
	CommRobotinoObjectsIDL::RobotinoDockingEventState idl_RobotinoDockingEventState;
	
public:
	// give a publicly accessible type-name for the template parameter IDL
	typedef CommRobotinoObjectsIDL::RobotinoDockingEventState DATATYPE;
	
	#ifdef ENABLE_HASH
		static size_t generateDataHash(const DATATYPE &);
	#endif
	
	static const char* getCompiledHash();
	static void getAllHashValues(std::list<std::string> &hashes);
	static void checkAllHashValues(std::list<std::string> &hashes);
	
	// default constructors
	RobotinoDockingEventStateCore();
	RobotinoDockingEventStateCore(const DATATYPE &data);
	// default destructor
	virtual ~RobotinoDockingEventStateCore();
	
	const DATATYPE& get() const { return idl_RobotinoDockingEventState; }
	operator const DATATYPE&() const { return idl_RobotinoDockingEventState; }
	DATATYPE& set() { return idl_RobotinoDockingEventState; }

	static inline std::string identifier(void) { return "CommRobotinoObjects::RobotinoDockingEventState"; }
	
	// helper method to easily implement output stream in derived classes
	void to_ostream(std::ostream &os = std::cout) const;
	
	// convert to xml stream
	void to_xml(std::ostream &os, const std::string &indent = "") const;
	
	// restore from xml stream
	void from_xml(std::istream &is);
	
	// User Interface
	
	// getter and setter for element NewState
	inline CommRobotinoObjects::RobotinoDockingEventType getNewState() const { return CommRobotinoObjects::RobotinoDockingEventType(idl_RobotinoDockingEventState.newState); }
	inline RobotinoDockingEventStateCore& setNewState(const CommRobotinoObjects::RobotinoDockingEventType &newState) { idl_RobotinoDockingEventState.newState = newState; return *this; }
};

} /* namespace CommRobotinoObjects */
#endif /* COMMROBOTINOOBJECTS_ROBOTINODOCKINGEVENTSTATE_CORE_H_ */
