//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMROBOTINOOBJECTS_PATHNAVIGATIONEVENTTYPE_ENUM_H_
#define COMMROBOTINOOBJECTS_PATHNAVIGATIONEVENTTYPE_ENUM_H_

#include <CommRobotinoObjects/enumPathNavigationEventTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommRobotinoObjects {
	
	class PathNavigationEventType {
	private:
		CommRobotinoObjectsIDL::PathNavigationEventType value;
		
	public:
		enum ENUM_PathNavigationEventType {
			ENUM_VALUE_UNDEFINED = 0,
			GOAL_UNKNOWN = 1,
			START_TOO_FAR = 2,
			GOAL_REACHED = 3,
			GOAL_NOT_REACHED = 4,
			NO_PATH_FOUND = 5,
			FATAL_ERROR = 6
		};
		
		// default constructor
		PathNavigationEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		PathNavigationEventType(ENUM_PathNavigationEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		PathNavigationEventType(CommRobotinoObjectsIDL::PathNavigationEventType e) {
			value = e;
		}
		
		//const CommRobotinoObjectsIDL::PathNavigationEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_PathNavigationEventType() const {
			return static_cast<ENUM_PathNavigationEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_PathNavigationEventType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case GOAL_UNKNOWN:
					result = "PathNavigationEventType::GOAL_UNKNOWN";
					break;
				case START_TOO_FAR:
					result = "PathNavigationEventType::START_TOO_FAR";
					break;
				case GOAL_REACHED:
					result = "PathNavigationEventType::GOAL_REACHED";
					break;
				case GOAL_NOT_REACHED:
					result = "PathNavigationEventType::GOAL_NOT_REACHED";
					break;
				case NO_PATH_FOUND:
					result = "PathNavigationEventType::NO_PATH_FOUND";
					break;
				case FATAL_ERROR:
					result = "PathNavigationEventType::FATAL_ERROR";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const PathNavigationEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
