//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// This file is generated once. Modify this file to your needs. 
// If you want the toolchain to re-generate this file, please 
// delete it before running the code generator.
//--------------------------------------------------------------------------

#include "CommRobotinoObjects/CommPathNavigationGoal.hh"

using namespace CommRobotinoObjects;

CommPathNavigationGoal::CommPathNavigationGoal()
:	CommPathNavigationGoalCore()
{  }


/**
 * Constructor to set all values.
 * NOTE that you have to keep this constructor consistent with the model!
 * Use  at your own choice.
 *
 * The preferred way to set values for initialization is:
 *      CommRepository::MyCommObject obj;
 *      obj.setX(1).setY(2).setZ(3)...;
CommPathNavigationGoal::CommPathNavigationGoal(const int &goalID, const CommRobotinoObjects::CommNavigationPaths &paths, const double &goalX, const double &goalY, const int &goal_id, const double &startX, const double &startY, const int &start_id)
:	CommPathNavigationGoalCore() // base constructor sets default values as defined in the model
{
	setGoalID(goalID);
	setPaths(paths);
	setGoalX(goalX);
	setGoalY(goalY);
	setGoal_id(goal_id);
	setStartX(startX);
	setStartY(startY);
	setStart_id(start_id);
}
 */


void CommPathNavigationGoal::setGoal(double startX,double startY, int startID, double goalX, double goalY, int goalID, const double unit){
	setStartX(startX * unit);
	setStartY(startY * unit);
	setStart_id(startID);

	setGoalX(goalX * unit);
	setGoalY(goalY * unit);
	setGoal_id(goalID);
}

void CommPathNavigationGoal::getGoal(double& startX,double& startY,int& startID, double& goalX, double &goalY,int& goalID, const double unit){
	startX = getStartX() / unit;
	startY = getStartY() / unit;
	startID = getStart_id();
	goalX = getGoalX() / unit;
	goalY = getGoalY() / unit;
	goalID = getGoal_id();
}


CommPathNavigationGoal::CommPathNavigationGoal(const CommPathNavigationGoalCore &commPathNavigationGoal)
:	CommPathNavigationGoalCore(commPathNavigationGoal)
{  }

CommPathNavigationGoal::CommPathNavigationGoal(const DATATYPE &commPathNavigationGoal)
:	CommPathNavigationGoalCore(commPathNavigationGoal)
{  }

CommPathNavigationGoal::~CommPathNavigationGoal()
{  }
